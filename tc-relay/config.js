'use strict';

// ─── تنظیمات اصلی ───────────────────────────────────────────────────────────
// لطفاً این مقادیر را قبل از اجرا پر کنید

module.exports = {
  // پورت TCP که دستگاه‌ها به آن وصل می‌شوند
  TCP_PORT: 2022,

  // ─── اطلاعات سامانه RMTO (RAHSAM) ───────────────────────────────────────
  RMTO_URL: 'http://REPLACE_WITH_RMTO_URL/Service.asmx',  // آدرس سرویس SOAP
  CID: 'REPLACE_WITH_CID',   // شناسه شرکت
  UID: 'REPLACE_WITH_UID',   // نام کاربری
  PWD: 'REPLACE_WITH_PWD',   // رمز عبور

  // ─── نگاشت sysId دستگاه به کد محور (RID) ────────────────────────────────
  // کلید: sysId هشت‌رقمی که دستگاه در پیام 8000 ارسال می‌کند
  // مقدار: RID محور در سامانه RMTO
  ROUTE_MAP: {
    // '00000001': 'R001',
    // '00000002': 'R002',
  },

  // ─── تنظیمات زمانی ──────────────────────────────────────────────────────
  // حداکثر اختلاف زمانی مجاز (میلی‌ثانیه) قبل از sync اجباری
  MAX_CLOCK_DRIFT_MS: 5 * 60 * 1000,    // 5 دقیقه
  WARN_CLOCK_DRIFT_MS: 2 * 60 * 1000,   // 2 دقیقه

  // فاصله polling داده (میلی‌ثانیه) — هر 5 دقیقه
  POLL_INTERVAL_MS: 5 * 60 * 1000,

  // فاصله time-sync مجدد (میلی‌ثانیه) — هر 15 دقیقه
  RESYNC_INTERVAL_MS: 15 * 60 * 1000,

  // تایم‌اوت انتظار برای ACK پس از time-sync (میلی‌ثانیه)
  ACK_TIMEOUT_MS: 30 * 1000,

  // ─── مسیر فایل لاگ ──────────────────────────────────────────────────────
  LOG_DIR: './logs',
};
